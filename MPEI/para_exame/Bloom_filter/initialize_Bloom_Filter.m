function bloomFilter = initialize_Bloom_Filter(n)
    % Inicializa um Bloom Filter de bits com zeros
    % input: n - tamanho do array
    % output: BF - array de bits
    
    
    bloomFilter = zeros(1, n, 'uint8');
end
